#include <stdio.h>

int main(){
    int arr[4] = {3, 2 , 2 ,5};
    int uniqueElements;

    for( int i = 0; i < sizeof(arr)/sizeof(int); i++){
        for( int j = 0; i < sizeof(arr)/sizeof(int); i++){
                if( i == j)continue;
                arr[]
        }
    }
}
